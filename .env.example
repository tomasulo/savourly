# Database Configuration
# For local development, use a local file:
TURSO_DATABASE_URL=file:savourly.db
TURSO_AUTH_TOKEN=

# For production with hosted Turso (required for Vercel):
# TURSO_DATABASE_URL=libsql://your-database.turso.io
# TURSO_AUTH_TOKEN=your-auth-token-here

# Authentication Configuration
# For development:
BETTER_AUTH_SECRET=dev-secret-change-in-production
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# For production (Vercel):
# BETTER_AUTH_SECRET=your-random-secret-here (generate with: openssl rand -base64 32)
# BETTER_AUTH_URL=https://your-domain.vercel.app
# NEXT_PUBLIC_BETTER_AUTH_URL=https://your-domain.vercel.app

# Vercel Deployment Notes:
# 1. Set all production environment variables in Vercel dashboard
# 2. TURSO_DATABASE_URL must use a hosted Turso database (libsql://...)
# 3. Generate BETTER_AUTH_SECRET with: openssl rand -base64 32
# 4. Update BETTER_AUTH_URL and NEXT_PUBLIC_BETTER_AUTH_URL to match your Vercel domain
